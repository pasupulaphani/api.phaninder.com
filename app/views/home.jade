extends layout/top

block content
	.row
		section(role='main')
			#post.row
				tinymcetextarea(name='content', style='width: 100%; height: 400px;')
					.eight.columns
						if !(posts && posts.length)
							p Nothing to disp ATM
						else
							ul.posts
								each post in posts
									li
										h2
											a(href="/posts/#{post.id}")= post.title

	include layout/bottom